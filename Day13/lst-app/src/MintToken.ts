
export const MintToken = async  (fromAddress : string, toAddress : string, amount:number )=>{
    console.log("minting token");
}

export const burnToken = async(fromAddress : string, toAddress : string, amount:number )=>{
    console.log("burning token");
}

export const sendNativeToken = async(fromAddress : string, toAddress : string, amount:number )=>{
    console.log("sending native token");
}